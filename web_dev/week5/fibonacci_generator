function* fibo(n){
    let a = 0, b = 1;
    for (let i = 0; i < n; i++) {
        yield a;
        let temp = a;
        a = b;
        b = temp + b;
    }
} 
const fib = fibo(5);
console.log(fib.next().value);
